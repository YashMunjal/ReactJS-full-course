{
  "name": "reac-full",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "format":"prettier --write \"src/**/*.{js,jsx}\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "yash munjal",
  "license": "ISC",
  "devDependencies": {
    "prettier": "^2.0.5"
  }
}
